{
  "name": "@swayam18/offline-geocoder",
  "version": "1.0.3",
  "main": "src/index.js",
  "engines": {
    "node": ">=4"
  },
  "dependencies": {
    "sqlite3": "^3.1.1"
  },
  "scripts": {
    "test": "eslint '**/*.js'",
    "postinstall": "mkdir -p data && cd data && source ../scripts/postinstall.sh",
    "generate": "mkdir -p data && cd data && source ../scripts/generate.sh"
  },
  "author": "Luca Spiller",
  "license": "MIT",
  "devDependencies": {
    "@treblefm/eslint-config": "^1.4.0",
    "babel-eslint": "^7.1.1",
    "eslint": "^3.14.1",
    "eslint-plugin-babel": "^4.0.1",
    "eslint-plugin-flowtype": "^2.30.0",
    "eslint-plugin-node": "^3.0.5"
  }
}
